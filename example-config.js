/**
 * Go to : https://apps.twitter.com/ and create an app. Create an access_token and
 * add these to the config. Change the filename into config.js
 */

var config = {
    consumer_key: '[API key]',
    consumer_secret: '[API secret]',
    access_token: '[Access token]',
    access_token_secret: '[Access token secret]'
};

module.exports = config;